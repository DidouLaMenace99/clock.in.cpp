#include <iostream>
#include <chrono>
#include <ctime>
#include <thread>
using namespace std;

int getSeconde(tm *time);
int getMinutes(tm *time);
int getHours(tm *time);
int getDay(tm *time);
int getMonth(tm *time);
int getYear(tm *time);
int getTime();
